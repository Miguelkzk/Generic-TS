import { Express } from "express";

export function loadRoutes(app: Express.Application): void {
    // Ejemplo de cómo cargar rutas
    //app.use("/api/examples", ExampleRouter); // Usa el router específico para un conjunto de rutas
    // Puedes añadir más rutas aquí
}
